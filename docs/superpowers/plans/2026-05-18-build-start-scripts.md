# Build Start Scripts Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add package scripts so the project can build a Bun-targeted runtime artifact and start from that artifact.

**Architecture:** Keep the change limited to `package.json`. `dev` continues to run TypeScript source with watch mode, `build` emits `dist/index.js`, and `start` runs the built artifact. Use Bun's external package mode so runtime dependencies such as grammY, LLM SDKs, and SQLite-related packages remain resolved from `node_modules` instead of being bundled into a fragile single file.

**Tech Stack:** Bun, TypeScript, npm package scripts.

---

## File Structure

- Modify: `package.json`
  - Add `build` script.
  - Update `start` script to run `dist/index.js`.
  - Leave `dev`, `test`, `typecheck`, `db:reset`, and `memory:inspect` unchanged.

## Provenance

- `package.json:6-13` currently defines package scripts and is the only file needed for the requested package-script-only change.
- `src/index.ts:17-20` supports `--migrate-only`, which can be used after build to verify the built artifact starts without requiring bot credentials.
- `src/config.ts:223-263` validates runtime credentials during normal startup, so verification should avoid a full bot start unless environment secrets are present.

### Task 1: Confirm the missing build script

**Files:**
- Read/verify: `package.json`

- [ ] **Step 1: Run the existing build command to confirm current failure**

Run: `bun run build`

Expected: FAIL because `package.json` does not define a `build` script.

- [ ] **Step 2: Confirm current scripts before editing**

Check that `package.json` contains this scripts block:

```json
"scripts": {
  "dev": "bun --watch src/index.ts",
  "start": "bun src/index.ts",
  "test": "bun test",
  "typecheck": "bunx tsc --noEmit",
  "db:reset": "rm -f data/agent.db data/agent.db-shm data/agent.db-wal && bun src/index.ts --migrate-only",
  "memory:inspect": "bun scripts/inspect-memory.ts"
}
```

### Task 2: Add build and built-artifact start scripts

**Files:**
- Modify: `package.json:6-13`

- [ ] **Step 1: Update the scripts block**

Replace the existing scripts block with:

```json
"scripts": {
  "dev": "bun --watch src/index.ts",
  "build": "bun build src/index.ts --outdir dist --target bun --packages external",
  "start": "bun dist/index.js",
  "test": "bun test",
  "typecheck": "bunx tsc --noEmit",
  "db:reset": "rm -f data/agent.db data/agent.db-shm data/agent.db-wal && bun src/index.ts --migrate-only",
  "memory:inspect": "bun scripts/inspect-memory.ts"
}
```

- [ ] **Step 2: Preserve JSON formatting**

Ensure `package.json` remains valid JSON with no trailing commas.

### Task 3: Verify the scripts

**Files:**
- Verify: `package.json`
- Generated by verification: `dist/index.js`

- [ ] **Step 1: Run type checking**

Run: `bun run typecheck`

Expected: PASS with exit code 0.

- [ ] **Step 2: Run the build script**

Run: `bun run build`

Expected: PASS and create `dist/index.js`.

- [ ] **Step 3: Verify the built artifact can execute the migration-only path**

Run: `bun dist/index.js --migrate-only`

Expected: PASS and print a line beginning with `Migration done:`.

- [ ] **Step 4: Review git diff**

Run: `git diff -- package.json`

Expected: diff only adds the `build` script and changes `start` to `bun dist/index.js`.

- [ ] **Step 5: Do not commit unless the user asks**

No commit command should be run automatically. If the user requests a commit later, stage only `package.json` and use the repository's normal commit workflow.

## Self-Review

- Spec coverage: The plan implements the approved package-scripts-only approach by modifying only `package.json`.
- Placeholder scan: No TBD/TODO placeholders remain.
- Type consistency: Script names are consistent across all tasks: `build`, `start`, `dev`, `test`, `typecheck`, `db:reset`, and `memory:inspect`.
